                                            </div>

                                            {/* INLINE WELL LIST â€” shown when Excel is loaded */}
                                            {batchView === 'list' && (
